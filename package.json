{
  "name": "e-choness",
  "author": "echo_ybl@hotmail.com",
  "homepage": "https://e-choness.github.io/ctrl-alt-deploy",
  "repository": {
    "type": "git",
    "url": "https://github.com/e-choness/ctrl-alt-deploy"
  },
  "bugs": {
    "url": "https://github.com/e-choness/ctrl-alt-deploy/issues"
  },
  "scripts": {
    "prep": "gem install bundler && bundle install",
    "dev": "bundle exec jekyll serve --watch --trace",
    "start": "npm run dev",
    "build": "bundle exec jekyll build",
    "min": "uglifyjs ./js/index.js -o ./js/index.min.js  --comments all"
  },
  "devDependencies": {
    "uglify-js": "^2.7.5"
  }
}
